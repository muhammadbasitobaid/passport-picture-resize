# Duplicate this to .env (all are optional)
APP_URL='http://localhost:3000' ## used for metadata base url and sitemap
GOOGLE_SITE_VERIFICATION_ID='' ## optional - for Google Search Console
BG_REMOVAL_URL='http://localhost:7001' ## MODNet microservice (services/bg-removal); defaults to localhost:7001
